import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "images.unsplash.com",
      "plus.unsplash.com",
      "res.cloudinary.com",
      "api.dicebear.com",
      "unsplash.com",
      "og.luma.com",
    ],
  },
};

export default nextConfig;
